pipeline {
  agent master {

    stages{
        stage("build")
        {
        echo 'in the build'
        }
    stage("test")
        {
        echo 'in the test automation'
        }

    stage("deploy")
        {
        echo 'in the deploy'
        }
      }
  }
}
