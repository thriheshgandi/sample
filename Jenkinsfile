pipeline {
  agent any {

    stages{
        stage("build")
        {
        echo 'in the build automation'
        }
    stage("test")
        {
        echo 'in the test automation'
        }

    stage("deploy")
        {
        echo 'in the deploy'
        }
      }
  }
}
